version: "3.8"

services:
  web:
    build: .
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./public:/usr/local/apache2/htdocs
      - ./certs:/etc/letsencrypt
    restart: always

volumes:
  certs:
